# 架构设计图

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Unity Scene                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────┐              ┌───────────────────┐     │
│  │  GameManager   │              │   Canvas (UI)     │     │
│  │  ┌──────────┐  │              │  ┌─────────────┐  │     │
│  │  │GameRunner│  │              │  │GamePresenter│  │     │
│  │  └─────┬────┘  │              │  └──────┬──────┘  │     │
│  │        │       │              │         │         │     │
│  └────────┼───────┘              │    ┌────▼─────┐   │     │
│           │                      │    │   UI     │   │     │
│           │                      │    │ Elements │   │     │
│           │                      │    └──────────┘   │     │
│           │                      │         │         │     │
│           │                      │    ┌────▼─────┐   │     │
│           │                      │    │PlayerView│   │     │
│           │                      │    │ Container│   │     │
│           │                      │    │   ┌──┐   │   │     │
│           │                      │    │   │  │   │   │     │
│           │                      │    └───┴──┴───┘   │     │
│           │                      └───────────────────┘     │
└───────────┼──────────────────────────────────────────────┘
            │
            │ Initialize
            ▼
┌─────────────────────────────────────────────────────────────┐
│                     Logic Layer (纯C#)                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────┐           │
│  │           GameEngine (Singleton)            │           │
│  │  ┌────────────┐     ┌──────────────────┐   │           │
│  │  │ GameState  │     │ GameEventManager │   │           │
│  │  ├────────────┤     ├──────────────────┤   │           │
│  │  │ Players    │     │ OnGameInit       │   │           │
│  │  │ Turn       │────>│ OnCardPlayed     │───┼──┐        │
│  │  │ Phase      │     │ OnChallenge      │   │  │        │
│  │  │ Pile       │     │ OnEliminated     │   │  │Events  │
│  │  │ ...        │     │ OnGameOver       │   │  │        │
│  │  └────────────┘     └──────────────────┘   │  │        │
│  │         │                                   │  │        │
│  │         │ Process Logic                     │  │        │
│  │    ┌────▼─────────┐                         │  │        │
│  │    │ StepOnce()   │                         │  │        │
│  │    │ Initialize() │                         │  │        │
│  │    │ IsGameOver() │                         │  │        │
│  │    └──────────────┘                         │  │        │
│  └─────────────────────────────────────────────┘  │        │
│                                                    │        │
└────────────────────────────────────────────────────┼────────┘
                                                     │
                           Events Flow               │
                                                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   Presentation Layer                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  GamePresenter receives events and updates UI:             │
│                                                             │
│  ┌──────────────┐                                          │
│  │ OnEnable()   │ ── Subscribe to all events               │
│  ├──────────────┤                                          │
│  │ OnCardPlayed │ ── ShowMessage("Player X played...")     │
│  │              │ ── UpdatePlayerView()                    │
│  │              │ ── PlayAnimation()                       │
│  ├──────────────┤                                          │
│  │ OnChallenge  │ ── ShowMessage("Challenge!")             │
│  │              │ ── RevealCard()                          │
│  ├──────────────┤                                          │
│  │OnEliminated  │ ── PlayDeathAnimation()                  │
│  │              │ ── UpdateAliveCount()                    │
│  ├──────────────┤                                          │
│  │ OnGameOver   │ ── ShowWinner()                          │
│  │              │ ── PlayVictoryAnimation()                │
│  └──────────────┘                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 数据流

### 游戏初始化流程
```
1. GameRunner.Start()
   │
   ├─> Create GameConfig
   │
   ├─> Create IAgent[] (AI players)
   │
   └─> GameEngine.Initialize(config, agents)
       │
       ├─> Build deck
       ├─> Shuffle cards
       ├─> Deal cards to players
       │
       └─> Events.TriggerGameInitialized(event)
           │
           └─> GamePresenter.OnGameInitialized()
               │
               ├─> Create PlayerView for each player
               │
               └─> Update UI (table rank, chamber count)
```

### 游戏步进流程
```
1. GameRunner.StepGame()
   │
   └─> GameEngine.StepOnce()
       │
       ├─ if Phase == Claim:
       │   │
       │   └─> ProcessClaimPhase()
       │       │
       │       ├─> Agent.ChooseClaimCard()
       │       ├─> Play card to pile
       │       │
       │       └─> Events.TriggerCardPlayed(event)
       │           │
       │           └─> GamePresenter.OnCardPlayed()
       │               ├─> Show message
       │               ├─> Play animation
       │               └─> Update hand count
       │
       └─ if Phase == Response:
           │
           └─> ProcessResponsePhase()
               │
               ├─ if Agent.DecideChallenge() == true:
               │   │
               │   └─> ResolveChallenge()
               │       │
               │       ├─> Reveal card
               │       ├─> Determine winner/loser
               │       ├─> Update chamber count
               │       │
               │       ├─> Events.TriggerChallenge(event)
               │       │   └─> GamePresenter: Show result
               │       │
               │       ├─> Events.TriggerPunishmentTrack(event)
               │       │   └─> GamePresenter: Update counter
               │       │
               │       └─ if hit bullet:
               │           │
               │           └─> Events.TriggerPlayerEliminated(event)
               │               └─> GamePresenter: Play death animation
               │
               └─ else (accept claim):
                   │
                   └─> Events.TriggerClaimAccepted(event)
                       └─> GamePresenter: Show acceptance message
```

---

## 类职责

### 逻辑层

| 类名 | 职责 | 依赖 |
|------|------|------|
| `GameEngine` | 游戏规则、状态管理、流程控制 | GameState, IAgent |
| `GameState` | 存储所有游戏数据 | PlayerData, Card |
| `GameEventManager` | 事件发布/订阅 | 无 |
| `PlayerData` | 玩家数据（纯数据） | Card |
| `Card` | 卡牌数据（纯数据） | Rank |
| `IAgent` | AI 接口 | GameState |

### 表现层

| 类名 | 职责 | 依赖 |
|------|------|------|
| `GamePresenter` | 监听事件、更新 UI | GameEngine, PlayerView |
| `PlayerView` | 显示单个玩家 UI | PlayerData, MMFeedbacks |
| `GameRunner` | 初始化、控制游戏流程 | GameEngine, GamePresenter |

---

## 事件系统

### 事件类型和触发时机

| 事件 | 触发时机 | 数据 |
|------|----------|------|
| `GameInitializedEvent` | 游戏开始 | Players[], TableRank, BulletSlot |
| `CardPlayedEvent` | 玩家出牌 | PlayerIndex, DeclaredRank, RemainingCards |
| `ClaimAcceptedEvent` | 接受声明 | ResponderIndex, ResponderName |
| `ChallengeEvent` | 质疑发生 | ChallengerIndex, ClaimantIndex, RevealedRank, WasTruthful |
| `PunishmentTrackEvent` | 惩罚轨推进 | ChamberCount, Hit |
| `PlayerEliminatedEvent` | 玩家淘汰 | PlayerIndex, PlayerName, AliveCount |
| `GameOverEvent` | 游戏结束 | WinnerIndex, WinnerName, IsDraw |
| `TurnChangedEvent` | 回合切换 | CurrentPlayerIndex, Phase |

### 订阅方式

```csharp
// 在 GamePresenter 或其他监听者中
void OnEnable()
{
    GameEngine.Instance.Events.OnCardPlayed += HandleCardPlayed;
}

void OnDisable()
{
    GameEngine.Instance.Events.OnCardPlayed -= HandleCardPlayed;
}

void HandleCardPlayed(CardPlayedEvent e)
{
    Debug.Log($"{e.PlayerName} played {e.DeclaredRank}");
    // Update UI...
}
```

---

## AI 决策流程

```
┌─────────────────────────────────────┐
│    GameEngine needs AI decision     │
└───────────────┬─────────────────────┘
                │
    ┌───────────▼────────────┐
    │  Pass GameState to AI  │
    └───────────┬────────────┘
                │
    ┌───────────▼──────────────────────────┐
    │                                      │
    │  AI analyzes:                        │
    │  - Own hand                          │
    │  - Table rank                        │
    │  - Chamber count                     │
    │  - Alive players                     │
    │  - Phase (Claim/Response)            │
    │                                      │
    └───────────┬──────────────────────────┘
                │
    ┌───────────▼───────────┐
    │  AI makes decision:   │
    │                       │
    │  if Claim Phase:      │
    │    return Card ID     │
    │                       │
    │  if Response Phase:   │
    │    return bool        │
    │    (challenge or not) │
    └───────────┬───────────┘
                │
    ┌───────────▼────────────┐
    │  GameEngine executes   │
    │  decision and triggers │
    │  appropriate events    │
    └────────────────────────┘
```

### AI 类型对比

| AI 类型 | 出牌策略 | 质疑策略 | 特点 |
|---------|----------|----------|------|
| `RandomBot` | 随机出牌 | 30% 固定概率质疑 | 简单、不可预测 |
| `CautiousBot` | 优先出真牌 | 有真牌时 10% 质疑<br>无真牌时 30% 质疑 | 保守、低风险 |
| `RecklessBot` | 随机出牌 | 有真牌时 50% 质疑<br>无真牌时 80% 质疑 | 激进、高风险 |

---

## 扩展点

### 1. 添加新 AI
```
实现 IAgent 接口 → 在 GameRunner.CreateBot() 中注册
```

### 2. 添加新事件
```
GameEvents.cs 定义事件类 → GameEventManager 添加事件 
→ GameEngine 触发事件 → GamePresenter 处理事件
```

### 3. 添加新 UI
```
创建 UI 元素 → 在 GamePresenter 添加引用 
→ 在事件处理中更新 UI
```

### 4. 添加游戏模式
```
继承 GameEngine 或创建新的 GameMode 类 
→ 在 GameRunner 中选择模式
```

---

## 性能考虑

### 优化点
- ✅ 事件系统：使用 C# 原生 event（零 GC）
- ✅ 对象复用：PlayerData、Card 在整个游戏中复用
- ✅ UI 更新：仅在事件触发时更新，非每帧轮询

### 潜在瓶颈
- PlayerView 创建（游戏开始时一次性）
- UI 更新（事件驱动，频率可控）
- AI 决策（简单逻辑，可忽略）

### 未来优化
- 对象池：卡牌 UI、消息提示
- 异步事件：将 UI 更新移到协程
- LOD：远处 PlayerView 降低更新频率

---

**这个架构设计确保了：**
- 🔧 **可维护性** - 清晰的分层和职责
- 🧪 **可测试性** - 逻辑层完全独立
- 🚀 **可扩展性** - 事件驱动设计
- ⚡ **性能** - 最小化不必要的更新
- 📚 **可读性** - 丰富的注释和文档
