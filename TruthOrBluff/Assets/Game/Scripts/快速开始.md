# 🚀 快速开始指南

## 5分钟搭建完整游戏

### 步骤 1：创建基础对象（2分钟）

**1.1 创建游戏管理器**
```
Hierarchy 右键 → Create Empty
重命名为 "GameManager"
Add Component → GameRunner (LiarsBar)
```

**配置 GameRunner：**
- Player Count: 4
- Table Rank: Q
- Bullet Slot: 4
- Copies Per Rank Per Player: 2
- Bot Types: 调整数组大小为 4，设置为 [1, 2, 0, 1]
- Auto Play: ☐（先手动测试）
- Step Interval: 1

---

### 步骤 2：创建 UI（1分钟）

**2.1 创建 Canvas**
```
Hierarchy 右键 → UI → Canvas
Canvas 设置：
- Render Mode: Screen Space - Overlay
- UI Scale Mode: Scale With Screen Size
- Reference Resolution: 1920x1080
```

**2.2 添加 GamePresenter**
```
选中 Canvas
Add Component → GamePresenter (LiarsBar)
```

**2.3 创建基础 UI 文本**
```
在 Canvas 下创建：

TextMeshPro - "GameStatus"
├─ 位置: (0, 400, 0)
├─ 字号: 48
└─ 对齐: 居中

TextMeshPro - "TableRank"  
├─ 位置: (0, 300, 0)
├─ 字号: 36
└─ 对齐: 居中

TextMeshPro - "ChamberCount"
├─ 位置: (0, 200, 0)
├─ 字号: 36
└─ 对齐: 居中

TextMeshPro - "Message"
├─ 位置: (0, -300, 0)
├─ 字号: 42
├─ 颜色: 黄色
└─ 对齐: 居中
```

**2.4 连接 UI 到 GamePresenter**
```
选中 Canvas
在 GamePresenter 组件中：
- Game Status Text → 拖入 GameStatus
- Table Rank Text → 拖入 TableRank
- Chamber Count Text → 拖入ChamberCount
- Message Text → 拖入 Message
```

---

### 步骤 3：创建玩家视图 Prefab（2分钟）

**3.1 创建 PlayerView Prefab**
```
Hierarchy 右键 → UI → Panel
重命名为 "PlayerView"
```

**3.2 设置 Panel**
```
RectTransform:
- Width: 300
- Height: 150

Image:
- Color: 半透明灰色 (R:50, G:50, B:50, A:180)
```

**3.3 添加子元素**
```
在 PlayerView 下创建：

TextMeshPro - "NameText"
├─ 位置: (0, 40, 0)
├─ 字号: 32
├─ 对齐: 居中
└─ 文本: "玩家名"

TextMeshPro - "HandCountText"
├─ 位置: (0, 0, 0)
├─ 字号: 28
├─ 对齐: 居中
└─ 文本: "手牌: 6"

Image - "StatusIndicator"
├─ 位置: (0, -40, 0)
├─ 大小: (100, 20)
└─ 颜色: 绿色
```

**3.4 添加 PlayerView 组件**
```
选中 PlayerView
Add Component → PlayerView (LiarsBar)

连接引用：
- Name Text → 拖入 NameText
- Hand Count Text → 拖入 HandCountText
- Status Indicator → 拖入 StatusIndicator
```

**3.5 保存为 Prefab**
```
创建文件夹: Assets/Game/Prefabs
将 PlayerView 从 Hierarchy 拖入 Prefabs 文件夹
删除场景中的 PlayerView
```

---

### 步骤 4：连接 Prefab 到 Presenter

**4.1 创建玩家容器**
```
在 Canvas 下创建 Empty GameObject
重命名为 "PlayerViewContainer"
位置: (0, 0, 0)

添加 Grid Layout Group 组件：
- Cell Size: (320, 160)
- Spacing: (20, 20)
- Start Axis: Horizontal
- Child Alignment: Middle Center
```

**4.2 连接到 GamePresenter**
```
选中 Canvas
在 GamePresenter 组件：
- Player View Container → 拖入 PlayerViewContainer
- Player View Prefab → 拖入 Prefabs/PlayerView
```

**完成！✅**

---

## 🎮 运行游戏

**方式 1：手动步进**
1. 点击 Play
2. 屏幕左上角会出现调试按钮
3. 点击 "步进" 或按 Space 键执行一步

**方式 2：自动播放**
1. 在 GameManager 的 GameRunner 组件
2. 勾选 Auto Play ✅
3. 调整 Step Interval（推荐 0.5 - 2 秒）
4. 点击 Play

---

## 🎨 美化建议（可选）

### 添加动画效果

**1. 安装 Feel（如果还没有）**
```
已经在项目中了，跳过
```

**2. 为 PlayerView 添加 Feedback**
```
选中 PlayerView Prefab
Add Component → MM Feedbacks
创建三个 Feedbacks：
- Play Card Feedback
- Eliminated Feedback  
- Win Feedback

在每个 Feedback 中添加：
- MM Feedback Scale
- MM Feedback Color
- MM Feedback Sound（如果有音效）
```

**3. 连接 Feedback**
```
在 PlayerView 组件：
- Play Card Feedback → 拖入对应的 MMFeedbacks
- Eliminated Feedback → 拖入对应的 MMFeedbacks
- Win Feedback → 拖入对应的 MMFeedbacks
```

### 改善视觉效果

**背景**
```
Canvas → 添加 Image (Stretch)
颜色: 深色（R:20, G:20, B:30）
或使用贴图
```

**玩家卡片圆角**
```
使用 Unity UI Extensions
或用圆角贴图替换 Image Sprite
```

**文本描边**
```
选中所有 TextMeshPro
在 Extra Settings:
- Outline: Enable ✅
- Outline Width: 0.2
- Outline Color: 黑色
```

---

## 🐛 常见问题

### Q: 看不到 PlayerView？
A: 检查：
1. PlayerView Prefab 是否正确连接到 GamePresenter
2. PlayerViewContainer 是否正确设置
3. Canvas 的 Scale 设置是否正确

### Q: 事件不触发？
A: 检查：
1. GamePresenter 的 OnEnable/OnDisable 是否正常
2. 在 Inspector 中查看 GameEngine.State 是否有数据
3. 启用 Show Debug Logs 查看日志

### Q: 按钮不工作？
A: 检查：
1. GameRunner 脚本是否附加到 GameObject
2. 在 Play 模式下点击左上角的 GUI 按钮
3. 查看 Console 是否有报错

### Q: UI 错位？
A: 检查：
1. Canvas Scaler 的 Reference Resolution
2. PlayerViewContainer 的 Grid Layout Group 设置
3. 各个 UI 元素的 Anchor 设置

---

## 📱 测试清单

运行前检查：
- ☐ GameManager 存在且有 GameRunner 组件
- ☐ Canvas 存在且有 GamePresenter 组件
- ☐ 所有 UI 文本都连接到 GamePresenter
- ☐ PlayerView Prefab 正确配置
- ☐ PlayerViewContainer 在场景中
- ☐ 没有编译错误

运行后检查：
- ☐ 游戏开始时创建了 4 个 PlayerView
- ☐ PlayerView 显示玩家名称和手牌数
- ☐ 出牌时消息正确显示
- ☐ 惩罚轨计数正确更新
- ☐ 玩家淘汰时状态改变
- ☐ 游戏结束显示获胜者

---

## 🎯 下一步

完成基础设置后，可以：
1. 调整 AI 配置，测试不同组合
2. 添加音效和粒子效果
3. 实现更丰富的动画
4. 创建主菜单和设置界面
5. 添加保存/加载功能

**祝你游戏开发顺利！** 🎉
